import { IconifyIconComponent } from './iconify-icon.directive';

describe('IconifyIconDirective', () => {
  it('should create an instance', () => {
    const directive = new IconifyIconComponent();
    expect(directive).toBeTruthy();
  });
});
